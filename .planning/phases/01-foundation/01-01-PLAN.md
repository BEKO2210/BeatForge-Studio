---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/App.css
  - src/index.css
  - src/vite-env.d.ts
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Project builds without errors using Vite"
    - "Development server runs locally with hot reload"
    - "Basic app shell renders in browser"
    - "TypeScript strict mode enabled with no errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "vite"
    - path: "vite.config.ts"
      provides: "Vite configuration"
      contains: "defineConfig"
    - path: "tsconfig.json"
      provides: "TypeScript configuration with strict mode"
      contains: "\"strict\": true"
    - path: "src/App.tsx"
      provides: "Main application component"
      min_lines: 10
    - path: "src/main.tsx"
      provides: "React entry point"
      contains: "createRoot"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React component import"
      pattern: "import.*App"
    - from: "index.html"
      to: "src/main.tsx"
      via: "script tag"
      pattern: "src/main.tsx"
---

<objective>
Set up Vite + React + TypeScript project with strict mode and basic app shell for BeatForge Studio.

Purpose: Establish the foundation for a browser-based music visualizer creator with proper tooling and type safety.
Output: Working development environment with hot reload, strict TypeScript, and deployable to GitHub Pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite + React + TypeScript project</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.node.json, index.html, src/main.tsx, src/vite-env.d.ts, .gitignore</files>
  <action>
    Initialize new Vite project with React and TypeScript template:

    1. Run: npm create vite@latest . -- --template react-ts
       (Use current directory since we're in the project root)

    2. Install dependencies: npm install

    3. Enable strict TypeScript in tsconfig.json:
       - Ensure "strict": true is set
       - Add "noUncheckedIndexedAccess": true for extra safety
       - Add "forceConsistentCasingInFileNames": true

    4. Configure Vite for GitHub Pages in vite.config.ts:
       - Set base: '/Meet-the-Beat-main/' (or use import.meta.env.BASE_URL pattern)
       - Add build outDir: 'dist'

    Do NOT use Create React App — Vite is the specified tool.
    Do NOT add any libraries beyond the React template defaults yet.
  </action>
  <verify>
    - npm run build completes without errors
    - npm run dev starts development server
    - TypeScript shows no errors: npx tsc --noEmit
  </verify>
  <done>
    - Vite project created with React + TypeScript
    - All dependencies installed
    - TypeScript strict mode enabled
    - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create basic app shell with component structure</name>
  <files>src/App.tsx, src/App.css, src/index.css</files>
  <action>
    Create minimal app shell that will serve as the foundation:

    1. Update src/App.tsx to be a clean starting point:
       - Remove Vite boilerplate (counter, logos)
       - Create simple container with app title "BeatForge Studio"
       - Add placeholder text indicating where visualizer canvas will go
       - Use functional component with TypeScript

    2. Update src/App.css:
       - Reset default styles
       - Add full-viewport container styles
       - Dark theme base (background: #0a0a0a, text: #ffffff)
       - Center content for placeholder view

    3. Update src/index.css:
       - CSS reset (box-sizing, margin, padding)
       - Set html/body to 100vh, no scroll
       - System font stack

    Keep it minimal — this is just the shell. No Canvas, no audio, no complex layouts yet.
    Use CSS modules or plain CSS (not styled-components or Tailwind yet).
  </action>
  <verify>
    - npm run dev shows app shell in browser
    - Title "BeatForge Studio" visible
    - Dark theme applied
    - No console errors
  </verify>
  <done>
    - Clean app shell created
    - Dark theme applied
    - Ready for Phase 2+ feature additions
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify development workflow and commit</name>
  <files>package.json</files>
  <action>
    Verify the complete development workflow:

    1. Ensure package.json scripts are correct:
       - "dev": "vite"
       - "build": "tsc -b && vite build"
       - "preview": "vite preview"
       - "lint": "eslint ." (if eslint included)

    2. Test the full cycle:
       - npm run dev (should start at localhost:5173)
       - npm run build (should create dist/ folder)
       - npm run preview (should serve built app)

    3. Verify hot reload:
       - Make a small change to App.tsx
       - Confirm browser updates without refresh

    4. Run TypeScript check:
       - npx tsc --noEmit should pass with no errors

    Do NOT add testing frameworks yet — that's for later phases.
    Do NOT add pre-commit hooks yet — keep it simple for now.
  </action>
  <verify>
    - All npm scripts work correctly
    - Hot reload functions
    - TypeScript strict mode passes
    - Build produces dist/ folder
  </verify>
  <done>
    - Development workflow verified
    - All commands functional
    - Ready for feature development
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors or warnings
- [ ] `npm run dev` starts server at localhost:5173
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] Browser shows "BeatForge Studio" with dark theme
- [ ] Hot reload works when editing App.tsx
- [ ] dist/ folder created after build
</verification>

<success_criteria>

- All 3 tasks completed
- All verification checks pass
- No TypeScript errors
- No console errors in browser
- Clean, minimal starting point for Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
